get:
  tags:
    - test
  summary: Описание
  operationId: testTest
  parameters:
    - $ref: '../parameters/EntityIdPath.yaml'
  responses:
    "200":
      description: Описание ответа
      content:
        application/json:
          schema:
            $ref: "../schemas/Dictionary.yaml"
